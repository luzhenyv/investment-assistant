# Next Steps

## Before your next real weekend run

- Update `config/demo/portfolio.yaml` with your actual holdings.
- Adjust `target_weights` in `config/demo/config.yaml` to your intended allocation.

## Possible directions after that

- **Backtest harness** — validate the rule thresholds against historical data so you can
  *trust* the rules, not just apply them.
- **v2 AI weekly write-up** — wire up an AI analyst that reads `output/weekly_report.json`
  and produces a narrative weekly report (explanation only, not decisions).

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Generated reports
output/

# Cached market data
data/cache/

# Real money — lives in its own local-only git repo, never pushed to this remote.
# See docs/REAL_PORTFOLIO_SETUP.md
/private/
data/options_snapshots

# Accumulating daily observation store (generated; the <profile>/stocks slice is real-portfolio
# derived). Back it up locally — never push it to this remote.
data/daily_observations/
.DS_Store

# Reference books / papers (large binaries, not source)
2412.20138v7.pdf
Quantitative Trading - How to Build Your Own Algorithmic Trading Business (Ernest P Chan).pdf

# One-off exploration scratch script
mu_overnight_backtest.py

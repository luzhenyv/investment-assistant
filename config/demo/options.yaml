# Option strategies — READ by the engine for monitoring (separate Options section in
# the weekly report). Metrics are underlying-based: DTE, moneyness, breakeven, max P/L,
# intrinsic value, and assignment risk are derived from strikes + the live underlying
# price. No option-price feed; P&L shown is an intrinsic-only floor.
#
# net_debit (per share, + = paid / - = received) is optional: if omitted it's computed
# from leg premiums minus credits_collected. `type` is a free-form display label.
strategies:
  - id: NVDA-leaps
    underlying: NVDA
    type: leaps
    opened: 2026-06-24
    net_debit: 42.00          # per share (long call debit)
    note: "LEAP — leveraged long-term core exposure"
    legs:
      - action: long
        right: call
        strike: 150
        expiry: 2028-01-21
        contracts: 1
        premium: 42.0

  - id: NVDA-bull-call-spread
    underlying: NVDA
    type: vertical
    opened: 2026-06-24
    net_debit: 7.50           # per share: 12.00 long − 4.50 short
    note: "bullish, defined risk"
    legs:
      - action: long
        right: call
        strike: 200
        expiry: 2026-09-18
        contracts: 1
        premium: 12.0
      - action: short
        right: call
        strike: 230
        expiry: 2026-09-18
        contracts: 1
        premium: 4.5
